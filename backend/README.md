# Homefinder Backend

Node.js/Express API for the Homefinder buyer portal. Handles authentication, user management, and favorites.

## Setup

1. Install dependencies: `npm install`
2. Set `.env` variables (see frontend README for details).
3. Run: `npm start`

## API Docs

See frontend README for endpoints and example flows.